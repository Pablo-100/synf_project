{% extends 'base.html.twig' %}

{% block title %}Accueil - Produits Frais et de Qualité{% endblock %}

{% block body %}
<div class="home-page-premium">
    <!-- Hero Premium -->
    <section class="hero-premium mb-5">
        <div class="container">
            <div class="row align-items-center g-5">
                <div class="col-lg-7">
                    <div class="hero-badge mb-3">
                        <i class="bi bi-lightning-charge-fill me-2"></i>Nouvelle expérience shopping
                    </div>
                    <h1 class="hero-title mb-4">Des produits frais, <span class="gradient-text">simplement</span>.</h1>
                    <p class="hero-subtitle mb-5">Qualité premium, prix justes, livrés chez vous en 24h. Découvrez l'expérience FreshMarket.</p>
                    <div class="hero-actions d-flex flex-wrap gap-3">
                        <a href="{{ path('app_product_index') }}" class="btn btn-primary btn-hero">
                            <i class="bi bi-grid-3x3-gap me-2"></i>Découvrir la boutique
                        </a>
                        {% if not app.user %}
                            <a href="{{ path('app_register') }}" class="btn btn-outline-light btn-hero">
                                <i class="bi bi-person-plus me-2"></i>Créer un compte
                            </a>
                        {% endif %}
                    </div>
                </div>
                <div class="col-lg-5">
                    <div class="hero-card">
                        <div class="hero-card-icon">
                            <i class="bi bi-truck"></i>
                        </div>
                        <div>
                            <div class="hero-card-label">Livraison Express</div>
                            <div class="hero-card-value">24h garantie</div>
                            <div class="hero-card-detail">Offerte dès 50€ d'achat</div>
                        </div>
                        <div class="hero-card-badge">
                            <i class="bi bi-check-circle-fill"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Premium -->
    <div class="container mb-6">
        <div class="text-center mb-5">
            <div class="section-badge mb-3">
                <i class="bi bi-star-fill me-2"></i>Nos Avantages
            </div>
            <h2 class="section-title mb-3">Pourquoi choisir <span class="gradient-text">FreshMarket</span></h2>
            <p class="section-subtitle">Qualité, fraîcheur et simplicité au quotidien</p>
        </div>
        
        <div class="row g-4">
            <div class="col-lg-3 col-md-6">
                <div class="feature-card">
                    <div class="feature-icon blue">
                        <i class="bi bi-truck"></i>
                    </div>
                    <h5 class="feature-title">Livraison Express</h5>
                    <p class="feature-text">Offerte dès 50€. Chez vous en 24h chrono.</p>
                    <div class="feature-arrow">
                        <i class="bi bi-arrow-right"></i>
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="feature-card">
                    <div class="feature-icon green">
                        <i class="bi bi-emoji-smile"></i>
                    </div>
                    <h5 class="feature-title">Produits Bio</h5>
                    <p class="feature-text">Sélection certifiée et locale, 100% traçable.</p>
                    <div class="feature-arrow">
                        <i class="bi bi-arrow-right"></i>
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="feature-card">
                    <div class="feature-icon orange">
                        <i class="bi bi-shield-check"></i>
                    </div>
                    <h5 class="feature-title">Qualité Premium</h5>
                    <p class="feature-text">Sélection rigoureuse par nos experts.</p>
                    <div class="feature-arrow">
                        <i class="bi bi-arrow-right"></i>
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="feature-card">
                    <div class="feature-icon cyan">
                        <i class="bi bi-cash-coin"></i>
                    </div>
                    <h5 class="feature-title">Prix Justes</h5>
                    <p class="feature-text">Promos quotidiennes, toute l'année.</p>
                    <div class="feature-arrow">
                        <i class="bi bi-arrow-right"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Catégories Premium -->
    <div class="container mb-6">
        <div class="section-header-inline mb-5">
            <div>
                <h2 class="section-title mb-2">
                    <i class="bi bi-grid-3x3-gap-fill me-2" style="color: var(--primary-color);"></i>
                    Nos Catégories
                </h2>
                <p class="section-subtitle mb-0">Explorez notre large sélection</p>
            </div>
            <a href="{{ path('app_product_index') }}" class="btn btn-outline-primary btn-premium-sm">
                Tout parcourir <i class="bi bi-arrow-right ms-2"></i>
            </a>
        </div>
        
        <div class="row g-3">
            {% set cats = [
                {icon:'bi-basket',name:'Fruits & Légumes', color:'green'},
                {icon:'bi-egg-fried',name:'Viandes', color:'red'},
                {icon:'bi-droplet-half',name:'Poissons', color:'blue'},
                {icon:'bi-cup-straw',name:'Boissons', color:'orange'},
                {icon:'bi-bread-slice',name:'Boulangerie', color:'brown'},
                {icon:'bi-bag',name:'Épicerie', color:'purple'},
                {icon:'bi-snow2',name:'Surgelés', color:'cyan'},
                {icon:'bi-star',name:'Promos', color:'yellow'}
            ] %}
            {% for c in cats %}
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="category-card">
                    <div class="category-icon {{ c.color }}">
                        <i class="bi {{ c.icon }}"></i>
                    </div>
                    <h6 class="category-name">{{ c.name }}</h6>
                    <div class="category-arrow">
                        <i class="bi bi-arrow-right"></i>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <!-- Products Premium -->
    <div class="container mb-6">
        <div class="section-header-inline mb-5">
            <div>
                <h2 class="section-title mb-2">
                    <i class="bi bi-star-fill me-2" style="color: #f59e0b;"></i>
                    Nos Produits en Vedette
                </h2>
                <p class="section-subtitle mb-0">Découvrez notre sélection du moment</p>
            </div>
            <a href="{{ path('app_product_index') }}" class="btn btn-outline-primary btn-premium-sm">
                Voir tout <i class="bi bi-arrow-right ms-2"></i>
            </a>
        </div>
        
        {% if products is empty %}
            <div class="empty-state-premium">
                <div class="empty-state-icon">🛒</div>
                <h4 class="empty-state-title">Nos produits arrivent bientôt...</h4>
                <p class="empty-state-text">Revenez prochainement pour découvrir notre sélection premium.</p>
            </div>
        {% else %}
            <div class="row g-4">
                {% for product in products|slice(0, 8) %}
                    <div class="col-lg-3 col-md-4 col-sm-6">
                        <div class="product-card-premium">
                            <div class="product-image-wrapper">
                                {% if product.image %}
                                    <img src="{{ asset('uploads/' ~ product.image) }}" 
                                         class="product-image" 
                                         alt="{{ product.nom }}">
                                {% else %}
                                    <div class="product-image-placeholder">
                                        <div class="product-emoji">🍽️</div>
                                    </div>
                                {% endif %}
                                
                                {% if product.stock < 5 and product.stock > 0 %}
                                    <span class="product-badge warning">
                                        <i class="bi bi-exclamation-circle me-1"></i>Stock Limité
                                    </span>
                                {% elseif product.stock == 0 %}
                                    <span class="product-badge danger">
                                        <i class="bi bi-x-circle me-1"></i>Épuisé
                                    </span>
                                {% else %}
                                    <span class="product-badge success">
                                        <i class="bi bi-check-circle me-1"></i>Disponible
                                    </span>
                                {% endif %}
                                
                                <div class="product-overlay">
                                    <a href="{{ path('app_product_show', {id: product.id}) }}" class="btn btn-light btn-sm rounded-pill">
                                        <i class="bi bi-eye me-1"></i>Voir détails
                                    </a>
                                </div>
                            </div>
                            
                            <div class="product-body">
                                <span class="product-category">{{ product.categorie }}</span>
                                <h5 class="product-title">{{ product.nom }}</h5>
                                <p class="product-description">
                                    {{ product.description|length > 70 ? product.description|slice(0, 70) ~ '...' : product.description }}
                                </p>
                                <div class="product-footer">
                                    <span class="product-price">{{ product.prix|number_format(2, ',', ' ') }} €</span>
                                    <a href="{{ path('app_product_show', {id: product.id}) }}" class="btn btn-primary btn-sm btn-add-cart">
                                        <i class="bi bi-cart-plus"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            {% if products|length > 8 %}
                <div class="text-center mt-5">
                    <a href="{{ path('app_product_index') }}" class="btn btn-primary btn-hero px-5">
                        <i class="bi bi-grid-3x3-gap me-2"></i>Découvrir Tous les Produits
                    </a>
                </div>
            {% endif %}
        {% endif %}
    </div>

    <!-- Promo Banner Premium -->
    <div class="container mb-6">
        <div class="promo-banner">
            <div class="promo-decoration"></div>
            <div class="row align-items-center g-4">
                <div class="col-lg-8">
                    <div class="promo-badge mb-3">
                        <i class="bi bi-gift-fill me-2"></i>Offre du Jour
                    </div>
                    <h3 class="promo-title mb-2">-20% sur fruits et légumes</h3>
                    <p class="promo-text mb-0">+ livraison offerte dès 30€ · Offre valable aujourd'hui uniquement</p>
                </div>
                <div class="col-lg-4 text-center text-lg-end">
                    <a href="{{ path('app_product_index') }}" class="btn btn-light btn-hero">
                        <i class="bi bi-arrow-right me-2"></i>Profiter de l'offre
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Stats Premium -->
    <div class="container mb-6">
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-value">15,000+</div>
                <div class="stat-label">Clients Satisfaits</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📦</div>
                <div class="stat-value">500+</div>
                <div class="stat-label">Produits Disponibles</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">⭐</div>
                <div class="stat-value">4.8/5</div>
                <div class="stat-label">Note Moyenne</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">🚚</div>
                <div class="stat-value">24h</div>
                <div class="stat-label">Livraison Express</div>
            </div>
        </div>
    </div>
</div>

{% block stylesheets %}
    {{ parent() }}
    <style>
        /* Home Page Premium Styles */
        .home-page-premium {
            overflow-x: hidden;
        }

        .mb-6 {
            margin-bottom: 5rem !important;
        }

        /* Hero Premium */
        .hero-premium {
            background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
            padding: 6rem 0;
            position: relative;
            overflow: hidden;
        }

        .hero-premium::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 800px;
            height: 800px;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            border-radius: 50%;
            animation: pulse 4s ease-in-out infinite;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 1.5rem;
            background: rgba(255,255,255,0.2);
            border-radius: 50px;
            color: white;
            font-weight: 600;
            font-size: 0.9rem;
            backdrop-filter: blur(10px);
            animation: fadeInScale 0.6s ease-out;
        }

        .hero-title {
            font-size: 4rem;
            font-weight: 800;
            color: white;
            line-height: 1.1;
            animation: fadeInScale 0.8s ease-out 0.2s both;
        }

        .gradient-text {
            background: linear-gradient(135deg, #fbbf24, #f59e0b);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-subtitle {
            font-size: 1.25rem;
            color: rgba(255,255,255,0.9);
            line-height: 1.6;
            animation: fadeInScale 1s ease-out 0.4s both;
        }

        .hero-actions {
            animation: fadeInScale 1.2s ease-out 0.6s both;
        }

        .btn-hero {
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.05rem;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .btn-hero:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        .btn-outline-light.btn-hero {
            border-color: rgba(255,255,255,0.3);
            background: transparent;
        }

        .btn-outline-light.btn-hero:hover {
            background: white;
            color: var(--primary-color);
            border-color: white;
        }

        .hero-card {
            background: white;
            border-radius: 24px;
            padding: 2.5rem;
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
            display: flex;
            align-items: center;
            gap: 1.5rem;
            animation: fadeInScale 1.4s ease-out 0.8s both;
            position: relative;
        }

        .hero-card-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: white;
            flex-shrink: 0;
        }

        .hero-card-label {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 0.25rem;
        }

        .hero-card-value {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .hero-card-detail {
            font-size: 0.85rem;
            color: var(--text-secondary);
        }

        .hero-card-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            color: #22c55e;
            font-size: 1.5rem;
        }

        /* Section Headers */
        .section-badge {
            display: inline-flex;
            align-items: center;
            padding: 0.5rem 1rem;
            background: rgba(59,130,246,0.1);
            border-radius: 50px;
            color: var(--primary-color);
            font-weight: 600;
            font-size: 0.85rem;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .section-subtitle {
            font-size: 1.1rem;
            color: var(--text-secondary);
        }

        .section-header-inline {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .btn-premium-sm {
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-premium-sm:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(59,130,246,0.3);
        }

        /* Feature Cards */
        .feature-card {
            background: var(--surface);
            border-radius: 20px;
            padding: 2rem;
            border: 2px solid var(--border-color);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            height: 100%;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-color), transparent);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.1);
            border-color: var(--primary-color);
        }

        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.75rem;
            color: white;
            margin-bottom: 1.5rem;
        }

        .feature-icon.blue { background: linear-gradient(135deg, #3b82f6, #60a5fa); }
        .feature-icon.green { background: linear-gradient(135deg, #22c55e, #4ade80); }
        .feature-icon.orange { background: linear-gradient(135deg, #f59e0b, #fbbf24); }
        .feature-icon.cyan { background: linear-gradient(135deg, #0ea5e9, #38bdf8); }

        .feature-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.75rem;
        }

        .feature-text {
            color: var(--text-secondary);
            margin-bottom: 0;
        }

        .feature-arrow {
            position: absolute;
            bottom: 1.5rem;
            right: 1.5rem;
            color: var(--primary-color);
            font-size: 1.25rem;
            opacity: 0;
            transform: translateX(-10px);
            transition: all 0.3s ease;
        }

        .feature-card:hover .feature-arrow {
            opacity: 1;
            transform: translateX(0);
        }

        /* Category Cards */
        .category-card {
            background: var(--surface);
            border-radius: 16px;
            padding: 1.75rem;
            border: 2px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 1rem;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
        }

        .category-card:hover {
            transform: translateX(5px);
            border-color: var(--primary-color);
            box-shadow: 0 8px 24px rgba(0,0,0,0.08);
        }

        .category-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: white;
            flex-shrink: 0;
        }

        .category-icon.green { background: linear-gradient(135deg, #22c55e, #4ade80); }
        .category-icon.red { background: linear-gradient(135deg, #ef4444, #f87171); }
        .category-icon.blue { background: linear-gradient(135deg, #3b82f6, #60a5fa); }
        .category-icon.orange { background: linear-gradient(135deg, #f59e0b, #fbbf24); }
        .category-icon.brown { background: linear-gradient(135deg, #92400e, #b45309); }
        .category-icon.purple { background: linear-gradient(135deg, #a855f7, #c084fc); }
        .category-icon.cyan { background: linear-gradient(135deg, #0ea5e9, #38bdf8); }
        .category-icon.yellow { background: linear-gradient(135deg, #eab308, #facc15); }

        .category-name {
            font-weight: 600;
            color: var(--text-primary);
            margin: 0;
            flex-grow: 1;
        }

        .category-arrow {
            color: var(--text-secondary);
            opacity: 0;
            transform: translateX(-5px);
            transition: all 0.3s ease;
        }

        .category-card:hover .category-arrow {
            opacity: 1;
            transform: translateX(0);
        }

        /* Product Cards Premium */
        .product-card-premium {
            background: var(--surface);
            border-radius: 20px;
            border: 2px solid var(--border-color);
            overflow: hidden;
            transition: all 0.3s ease;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .product-card-premium:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 50px rgba(0,0,0,0.12);
            border-color: var(--primary-color);
        }

        .product-image-wrapper {
            position: relative;
            overflow: hidden;
            height: 250px;
        }

        .product-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-card-premium:hover .product-image {
            transform: scale(1.1);
        }

        .product-image-placeholder {
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(59,130,246,0.1), rgba(147,197,253,0.1));
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .product-emoji {
            font-size: 5rem;
        }

        .product-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
            backdrop-filter: blur(10px);
        }

        .product-badge.success {
            background: rgba(34,197,94,0.9);
            color: white;
        }

        .product-badge.warning {
            background: rgba(245,158,11,0.9);
            color: white;
        }

        .product-badge.danger {
            background: rgba(239,68,68,0.9);
            color: white;
        }

        .product-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.6);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .product-card-premium:hover .product-overlay {
            opacity: 1;
        }

        .product-body {
            padding: 1.5rem;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .product-category {
            display: inline-block;
            padding: 0.375rem 0.875rem;
            background: rgba(59,130,246,0.1);
            color: var(--primary-color);
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .product-title {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.75rem;
        }

        .product-description {
            font-size: 0.9rem;
            color: var(--text-secondary);
            flex-grow: 1;
            margin-bottom: 1rem;
        }

        .product-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 1rem;
            border-top: 2px solid var(--border-color);
        }

        .product-price {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .btn-add-cart {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0;
            transition: all 0.3s ease;
        }

        .btn-add-cart:hover {
            transform: rotate(15deg) scale(1.1);
        }

        /* Promo Banner Premium */
        .promo-banner {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            border-radius: 24px;
            padding: 3rem 2.5rem;
            position: relative;
            overflow: hidden;
            color: white;
        }

        .promo-decoration {
            position: absolute;
            top: -50px;
            right: -50px;
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            border-radius: 50%;
        }

        .promo-badge {
            display: inline-flex;
            align-items: center;
            padding: 0.5rem 1rem;
            background: rgba(255,255,255,0.2);
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.85rem;
        }

        .promo-title {
            font-size: 2rem;
            font-weight: 700;
        }

        .promo-text {
            font-size: 1.05rem;
            opacity: 0.95;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
        }

        .stat-card {
            background: var(--surface);
            border-radius: 20px;
            padding: 2.5rem 2rem;
            text-align: center;
            border: 2px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 35px rgba(0,0,0,0.1);
            border-color: var(--primary-color);
        }

        .stat-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .stat-value {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1rem;
            color: var(--text-secondary);
        }

        /* Empty State */
        .empty-state-premium {
            text-align: center;
            padding: 5rem 2rem;
        }

        .empty-state-icon {
            font-size: 6rem;
            opacity: 0.3;
            margin-bottom: 2rem;
        }

        .empty-state-title {
            font-size: 1.75rem;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 1rem;
        }

        .empty-state-text {
            color: var(--text-secondary);
            font-size: 1.05rem;
        }

        /* Animations */
        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: translateY(20px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        @keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.6;
            }
        }

        /* Responsive */
        @media (max-width: 992px) {
            .hero-title {
                font-size: 3rem;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .section-header-inline {
                flex-direction: column;
                align-items: flex-start;
                gap: 1.5rem;
            }
        }

        @media (max-width: 768px) {
            .hero-premium {
                padding: 4rem 0;
            }
            
            .hero-title {
                font-size: 2.5rem;
            }
            
            .hero-subtitle {
                font-size: 1.1rem;
            }
            
            .promo-banner {
                padding: 2rem 1.5rem;
            }
            
            .promo-title {
                font-size: 1.5rem;
            }
        }
    </style>
{% endblock %}
{% endblock %}
