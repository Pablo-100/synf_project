{% extends 'base.html.twig' %}

{% block title %}Nouveau Produit{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4"><i class="bi bi-plus-circle"></i> Ajouter un produit</h1>

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

    <a href="{{ path('app_admin_products') }}" class="btn btn-link mt-3">← Retour</a>
</div>
{% endblock %}