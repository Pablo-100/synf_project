<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Bienvenue{% endblock %} - Mon Site</title>
    
    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
        <style>
            /* Calm Light Palette */
            :root {
                --primary-color: #3b82f6;      /* Blue 500 calm */
                --primary-light: #93c5fd;      /* Blue 300 */
                --primary-dark: #2563eb;       /* Blue 600 */
                --background: #ffffff;         /* Pure white */
                --background-alt: #f8fafc;     /* Subtle gray 50 */
                --surface: #ffffff;            /* Cards/containers */
                --text-primary: #0f172a;       /* Slate 900 */
                --text-secondary: #475569;     /* Slate 600 */
                --border-color: #e2e8f0;       /* Gray 200 */
                --shadow: 0 1px 2px rgba(0,0,0,0.04), 0 4px 12px rgba(0,0,0,0.06);
                --shadow-hover: 0 8px 24px rgba(0,0,0,0.10);
            }

            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                transition: background-color 0.25s ease, color 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
            }

            body {
                font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
                color: var(--text-primary);
                background-color: var(--background);
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                line-height: 1.6;
                -webkit-font-smoothing: antialiased;
                -moz-osx-font-smoothing: grayscale;
            }

            h1, h2, h3, h4, h5, h6 {
                font-weight: 700;
                color: var(--text-primary);
                letter-spacing: -0.02em;
            }

            .brand-text { font-weight: 700; letter-spacing: -0.02em; }

            main { flex: 1; }

            /* (Dark mode removed intentionally) */
            
            /* ===== NAVIGATION PREMIUM ===== */
            .navbar-premium {
                background: rgba(255,255,255,0.95) !important;
                backdrop-filter: saturate(180%) blur(20px);
                -webkit-backdrop-filter: saturate(180%) blur(20px);
                border-bottom: 1px solid var(--border-color);
                padding: 1rem 0;
                box-shadow: 0 2px 20px rgba(0,0,0,0.04);
                transition: all 0.3s ease;
            }

            .navbar-premium.scrolled {
                padding: 0.5rem 0;
                box-shadow: 0 4px 30px rgba(0,0,0,0.08);
            }
            
            /* Brand Premium */
            .navbar-brand-premium {
                display: flex;
                align-items: center;
                gap: 0.75rem;
                text-decoration: none;
                transition: all 0.3s ease;
            }

            .navbar-brand-premium:hover {
                transform: translateY(-2px);
            }

            .brand-icon-wrapper {
                width: 45px;
                height: 45px;
                background: linear-gradient(135deg, var(--primary-color), #60a5fa);
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                color: white;
                font-size: 1.5rem;
                box-shadow: 0 4px 15px rgba(59,130,246,0.3);
                transition: all 0.3s ease;
            }

            .navbar-brand-premium:hover .brand-icon-wrapper {
                transform: rotate(-5deg) scale(1.05);
                box-shadow: 0 6px 20px rgba(59,130,246,0.4);
            }

            .brand-name {
                font-size: 1.5rem;
                font-weight: 800;
                color: var(--text-primary);
                letter-spacing: -0.02em;
            }

            .brand-highlight {
                color: var(--primary-color);
            }

            /* Navbar Toggler Premium */
            .navbar-toggler-premium {
                border: none;
                padding: 0;
                width: 30px;
                height: 24px;
                position: relative;
                background: transparent;
                cursor: pointer;
            }

            .navbar-toggler-premium span {
                display: block;
                width: 100%;
                height: 3px;
                background: var(--primary-color);
                border-radius: 3px;
                transition: all 0.3s ease;
                position: absolute;
            }

            .navbar-toggler-premium span:nth-child(1) { top: 0; }
            .navbar-toggler-premium span:nth-child(2) { top: 50%; transform: translateY(-50%); }
            .navbar-toggler-premium span:nth-child(3) { bottom: 0; }

            .navbar-toggler-premium:hover span {
                background: var(--primary-dark);
            }

            /* Nav Links Premium */
            .navbar-nav-premium {
                display: flex;
                gap: 0.5rem;
                list-style: none;
                padding: 0;
                margin: 0;
                align-items: center;
            }

            .nav-item-premium {
                list-style: none;
            }

            .nav-link-premium {
                display: flex;
                align-items: center;
                gap: 0.5rem;
                padding: 0.75rem 1.25rem;
                color: var(--text-primary);
                text-decoration: none;
                font-weight: 600;
                font-size: 0.95rem;
                border-radius: 12px;
                transition: all 0.3s ease;
                position: relative;
            }

            .nav-link-premium i {
                font-size: 1.1rem;
                transition: transform 0.3s ease;
            }

            .nav-link-premium:hover {
                color: var(--primary-color);
                background: rgba(59,130,246,0.08);
            }

            .nav-link-premium:hover i {
                transform: scale(1.15);
            }

            .nav-link-premium.active {
                color: white;
                background: linear-gradient(135deg, var(--primary-color), #60a5fa);
                box-shadow: 0 4px 15px rgba(59,130,246,0.3);
            }

            .nav-link-premium.active:hover {
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(59,130,246,0.4);
            }

            /* Nav Actions Premium */
            .navbar-actions-premium {
                display: flex;
                gap: 1rem;
                list-style: none;
                padding: 0;
                margin: 0;
                align-items: center;
            }

            /* Search Form Premium */
            .search-form-premium {
                margin: 0;
            }

            .search-wrapper {
                position: relative;
                width: 250px;
            }

            .search-icon {
                position: absolute;
                left: 1rem;
                top: 50%;
                transform: translateY(-50%);
                color: var(--text-secondary);
                font-size: 1rem;
                z-index: 1;
            }

            .search-input-premium {
                width: 100%;
                padding: 0.75rem 1rem 0.75rem 2.75rem;
                border: 2px solid var(--border-color);
                border-radius: 50px;
                font-size: 0.9rem;
                transition: all 0.3s ease;
                background: var(--surface);
                color: var(--text-primary);
            }

            .search-input-premium:focus {
                outline: none;
                border-color: var(--primary-color);
                box-shadow: 0 0 0 4px rgba(59,130,246,0.1);
                background: white;
            }

            .search-input-premium::placeholder {
                color: var(--text-secondary);
            }

            /* Nav Buttons Premium */
            .btn-nav-premium {
                display: inline-flex;
                align-items: center;
                gap: 0.5rem;
                padding: 0.75rem 1.5rem;
                border-radius: 50px;
                font-weight: 600;
                font-size: 0.9rem;
                text-decoration: none;
                transition: all 0.3s ease;
                border: 2px solid transparent;
            }

            .btn-nav-cart {
                position: relative;
                padding: 0.75rem 1rem;
                color: var(--primary-color);
                border: 2px solid var(--primary-color);
                background: transparent;
            }

            .btn-nav-cart:hover {
                color: white;
                background: var(--primary-color);
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(59,130,246,0.3);
            }

            .btn-nav-cart i {
                font-size: 1.4rem;
            }

            .cart-badge {
                position: absolute;
                top: -5px;
                right: -5px;
                background: linear-gradient(135deg, #ef4444, #dc2626);
                color: white;
                font-size: 0.7rem;
                font-weight: 700;
                width: 22px;
                height: 22px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                border: 2px solid white;
                box-shadow: 0 2px 8px rgba(239,68,68,0.4);
                animation: pulse 2s infinite;
            }

            @keyframes pulse {
                0%, 100% { transform: scale(1); }
                50% { transform: scale(1.1); }
            }

            .btn-nav-outline {
                color: var(--primary-color);
                border-color: var(--primary-color);
                background: transparent;
            }

            .btn-nav-outline:hover {
                color: white;
                background: var(--primary-color);
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(59,130,246,0.3);
            }

            .btn-nav-solid {
                color: white;
                background: linear-gradient(135deg, var(--primary-color), #60a5fa);
                box-shadow: 0 4px 15px rgba(59,130,246,0.3);
            }

            .btn-nav-solid:hover {
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(59,130,246,0.4);
            }

            /* ===== FOOTER PREMIUM ===== */
            .footer-premium {
                background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
                color: #e2e8f0;
                padding: 0;
                margin-top: 6rem;
                position: relative;
                overflow: hidden;
            }

            .footer-premium::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 1px;
                background: linear-gradient(90deg, transparent, rgba(59,130,246,0.5), transparent);
            }

            .footer-main {
                padding: 4rem 0 3rem;
                position: relative;
            }

            /* Footer Brand */
            .footer-brand {
                margin-bottom: 2rem;
            }

            .footer-logo {
                display: flex;
                align-items: center;
                gap: 0.75rem;
                margin-bottom: 1.5rem;
            }

            .footer-logo-icon {
                width: 45px;
                height: 45px;
                background: linear-gradient(135deg, var(--primary-color), #60a5fa);
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                color: white;
                font-size: 1.5rem;
                box-shadow: 0 4px 15px rgba(59,130,246,0.3);
            }

            .footer-logo-text {
                font-size: 1.5rem;
                font-weight: 800;
                color: white;
                letter-spacing: -0.02em;
            }

            .footer-logo-text .highlight {
                color: var(--primary-color);
            }

            .footer-description {
                font-size: 0.95rem;
                line-height: 1.7;
                color: #94a3b8;
                margin-bottom: 2rem;
            }

            /* Social Links */
            .social-links-premium {
                display: flex;
                gap: 1rem;
            }

            .social-link {
                width: 45px;
                height: 45px;
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(255,255,255,0.05);
                border: 1px solid rgba(255,255,255,0.1);
                border-radius: 12px;
                color: #e2e8f0;
                font-size: 1.2rem;
                text-decoration: none;
                transition: all 0.3s ease;
            }

            .social-link:hover {
                background: var(--primary-color);
                border-color: var(--primary-color);
                color: white;
                transform: translateY(-5px);
                box-shadow: 0 6px 20px rgba(59,130,246,0.4);
            }

            /* Footer Titles */
            .footer-title {
                font-size: 1.1rem;
                font-weight: 700;
                color: white;
                margin-bottom: 1.5rem;
                position: relative;
                padding-bottom: 0.75rem;
            }

            .footer-title::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 40px;
                height: 3px;
                background: linear-gradient(90deg, var(--primary-color), transparent);
                border-radius: 2px;
            }

            /* Footer Links */
            .footer-links {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .footer-links li {
                margin-bottom: 0.75rem;
            }

            .footer-links a {
                display: flex;
                align-items: center;
                gap: 0.5rem;
                color: #94a3b8;
                text-decoration: none;
                font-size: 0.95rem;
                transition: all 0.3s ease;
            }

            .footer-links a i {
                font-size: 0.75rem;
                transition: transform 0.3s ease;
            }

            .footer-links a:hover {
                color: var(--primary-color);
                padding-left: 0.5rem;
            }

            .footer-links a:hover i {
                transform: translateX(3px);
            }

            /* Footer Contact */
            .footer-contact {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .footer-contact li {
                display: flex;
                gap: 1rem;
                margin-bottom: 1.25rem;
            }

            .footer-contact i {
                width: 40px;
                height: 40px;
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(59,130,246,0.1);
                border-radius: 10px;
                color: var(--primary-color);
                font-size: 1.1rem;
                flex-shrink: 0;
            }

            .footer-contact div {
                flex: 1;
            }

            .footer-contact strong {
                display: block;
                color: white;
                font-size: 0.9rem;
                margin-bottom: 0.25rem;
            }

            .footer-contact span {
                display: block;
                color: #94a3b8;
                font-size: 0.9rem;
            }

            /* Footer Bottom */
            .footer-bottom {
                padding: 2rem 0;
                border-top: 1px solid rgba(255,255,255,0.1);
            }

            .footer-copyright {
                color: #94a3b8;
                font-size: 0.9rem;
                margin: 0;
            }

            .footer-copyright strong {
                color: white;
            }

            .footer-legal {
                display: flex;
                justify-content: center;
                gap: 2rem;
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .footer-legal a {
                color: #94a3b8;
                text-decoration: none;
                font-size: 0.9rem;
                transition: color 0.3s ease;
            }

            .footer-legal a:hover {
                color: var(--primary-color);
            }

            /* Responsive Nav */
            @media (max-width: 991px) {
                .navbar-nav-premium {
                    flex-direction: column;
                    width: 100%;
                    padding: 1rem 0;
                }

                .nav-link-premium {
                    width: 100%;
                    justify-content: flex-start;
                }

                .navbar-actions-premium {
                    flex-direction: column;
                    width: 100%;
                    padding: 1rem 0;
                }

                .search-wrapper {
                    width: 100%;
                }

                .btn-nav-premium {
                    width: 100%;
                    justify-content: center;
                }

                .footer-legal {
                    flex-direction: column;
                    gap: 0.75rem;
                    text-align: center;
                }
            }

            @media (max-width: 768px) {
                .footer-main {
                    padding: 3rem 0 2rem;
                }

                .footer-copyright {
                    text-align: center;
                    margin-bottom: 1rem;
                }
            }
            
            /* Navigation (old styles removed) */
            .navbar {
                background: rgba(255,255,255,0.7) !important;
                backdrop-filter: saturate(180%) blur(12px);
                -webkit-backdrop-filter: saturate(180%) blur(12px);
                border-bottom: 1px solid var(--border-color);
                padding: 0.75rem 0;
                box-shadow: none;
            }
            
            
            .navbar-brand {
                font-size: 1.4rem;
                color: var(--text-primary) !important;
                font-weight: 700;
                letter-spacing: -0.02em;
            }
            
            .navbar-brand i {
                color: var(--primary-color);
                margin-right: 8px;
            }
            
            .nav-link {
                color: var(--text-primary) !important;
                font-weight: 500;
                padding: 0.5rem 1rem !important;
                margin: 0 0.2rem;
                transition: all 0.3s ease;
                position: relative;
            }
            
            .nav-link:hover, .nav-link.active {
                color: var(--primary-color) !important;
            }
            
            .nav-link.active::after {
                width: 80%;
            }
            
            .nav-link::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 50%;
                width: 0;
                height: 2px;
                background: var(--primary-color);
                transition: all 0.3s ease;
                transform: translateX(-50%);
            }
            
            .nav-link:hover::after {
                width: 80%;
            }
            
            /* Boutons */
            .btn {
                font-weight: 500;
                padding: 0.75rem 2rem;
                border-radius: 50px;
                transition: all 0.3s ease;
                text-transform: uppercase;
                letter-spacing: 0.5px;
                font-size: 0.9rem;
                border: none;
                position: relative;
                overflow: hidden;
            }
            
            .btn::before {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                width: 0;
                height: 0;
                border-radius: 50%;
                background: rgba(255,255,255,0.2);
                transform: translate(-50%, -50%);
                transition: width 0.6s, height 0.6s;
            }
            
            .btn:hover::before {
                width: 300px;
                height: 300px;
            }
            
            .btn-primary { background: var(--primary-color); color: #fff; box-shadow: none; }
            
            .btn-primary:hover {
                transform: translateY(-1px);
                box-shadow: var(--shadow);
                filter: brightness(0.98);
            }
            
            .btn-outline-primary {
                border: 1.5px solid var(--primary-color);
                color: var(--primary-color);
                background: transparent;
            }
            
            .btn-outline-primary:hover {
                background: var(--primary-color);
                color: white;
                transform: translateY(-1px);
            }
            
            .btn-secondary { background: var(--background-alt); color: var(--text-primary); border: 1px solid var(--border-color); }
            .btn-secondary:hover { background: var(--surface); box-shadow: var(--shadow); }
            
            /* Cards */
            .card {
                border: 1px solid var(--border-color);
                border-radius: 14px;
                overflow: hidden;
                box-shadow: none;
                transition: all 0.4s ease;
                background: var(--surface);
            }
            
            
            .card:hover {
                transform: translateY(-4px);
                box-shadow: var(--shadow-hover);
                border-color: var(--primary-color);
            }
            
            
            .card-img-top {
                transition: transform 0.4s ease;
            }
            
            .card:hover .card-img-top {
                transform: scale(1.08);
            }
            
            .card-body {
                padding: 1.5rem;
                background: var(--surface);
            }
            
            .card-title {
                font-size: 1.2rem;
                margin-bottom: 1rem;
                color: var(--text-primary);
            }
            
            .card-text {
                color: var(--text-secondary);
            }
            
            /* Badge */
            .badge {
                padding: 0.5rem 1rem;
                border-radius: 50px;
                font-weight: 500;
                letter-spacing: 0.5px;
            }
            
            .badge.bg-primary { background: var(--primary-color) !important; }
            .badge.bg-success { background: #22c55e !important; }
            .badge.bg-warning { background: #f59e0b !important; }
            .badge.bg-danger { background: #ef4444 !important; }

            /* Readability fixes */
            .text-muted { color: var(--text-secondary) !important; }
            .bg-secondary { background-color: var(--background-alt) !important; color: var(--text-primary) !important; }

            /* Contact / cards general text */
            .card h1, .card h2, .card h3, .card h4, .card h5, .card h6 { color: var(--text-primary); }
            .card p { color: var(--text-secondary); margin-bottom: 0.5rem; }

            /* Form labels & placeholders */
            .form-label { color: var(--text-primary); }
            ::placeholder { color: var(--text-secondary); opacity: 1; }
            
            /* Footer */
            footer {
                background: var(--surface);
                color: var(--text-primary);
                padding: 2rem 0;
                margin-top: 3rem;
                border-top: 1px solid var(--border-color);
                box-shadow: none;
            }
            
            footer h5 {
                color: var(--primary-color);
                margin-bottom: 1.5rem;
                font-size: 1.2rem;
            }
            
            footer a {
                color: var(--text-secondary);
                text-decoration: none;
                transition: all 0.3s ease;
            }
            
            footer a:hover {
                color: var(--primary-color);
                padding-left: 5px;
            }
            
            footer .social-links a {
                display: inline-block;
                width: 45px;
                height: 45px;
                line-height: 45px;
                text-align: center;
                border-radius: 50%;
                background: var(--background-alt);
                color: var(--text-primary);
                transition: all 0.3s ease;
            }
            
            footer .social-links a:hover {
                background: var(--primary-color);
                color: white;
                transform: translateY(-5px);
                padding-left: 0;
            }
            
            /* Alerts */
            .alert {
                border: none;
                border-radius: 10px;
                padding: 1rem 1.5rem;
                box-shadow: var(--shadow);
                background: var(--surface);
                color: var(--text-primary);
                border-left: 4px solid;
            }
            
            .alert-success {
                border-left-color: #4caf50;
            }
            
            .alert-info {
                border-left-color: #2196f3;
            }
            
            .alert-warning {
                border-left-color: #ff9800;
            }
            
            .alert-danger {
                border-left-color: #f44336;
            }
            
            /* Formulaires */
            .form-control, .form-select {
                border: 2px solid var(--border-color);
                border-radius: 10px;
                padding: 0.75rem 1rem;
                transition: all 0.3s ease;
                background: var(--surface);
                color: var(--text-primary);
            }
            
            .form-control:focus, .form-select:focus {
                border-color: var(--primary-color);
                box-shadow: 0 0 0 0.2rem rgba(79, 70, 229, 0.15);
                background: var(--surface);
                color: var(--text-primary);
            }
            
            /* Dropdown */
            .dropdown-menu {
                border: 1px solid var(--border-color);
                box-shadow: var(--shadow);
                border-radius: 10px;
                padding: 0.5rem;
                background: var(--surface);
            }
            
            .dropdown-item {
                border-radius: 8px;
                padding: 0.7rem 1rem;
                transition: all 0.3s ease;
                color: var(--text-primary);
            }
            
            .dropdown-item:hover {
                background: var(--background-alt);
                color: var(--primary-color);
                padding-left: 1.5rem;
            }
            
            /* Tables */
            .table {
                box-shadow: var(--shadow);
                border-radius: 10px;
                overflow: hidden;
                background: var(--surface);
                color: var(--text-primary);
            }
            
            .table thead {
                background: #eef2ff; /* calm tint */
                color: var(--text-primary);
            }
            
            .table tbody tr {
                background: var(--surface);
                border-bottom: 1px solid var(--border-color);
            }
            
            .table tbody tr:hover { background: rgba(59,130,246,0.06); }

            /* Normalize table borders */
            .table, .table td, .table th { border-color: var(--border-color) !important; }

            /* Normalize Bootstrap colored card-headers to neutral surface + colored border-left */
            .card-header[class*="bg-"] {
                background: var(--surface) !important;
                color: var(--text-primary) !important;
                border-bottom: 1px solid var(--border-color) !important;
                position: relative;
                padding-top: .9rem; padding-bottom: .9rem;
            }
            .card-header.bg-primary { border-left: 4px solid var(--primary-color); }
            .card-header.bg-success { border-left: 4px solid #22c55e; }
            .card-header.bg-warning { border-left: 4px solid #f59e0b; }
            .card-header.bg-info { border-left: 4px solid #0ea5e9; }
            .card-header.bg-danger { border-left: 4px solid #ef4444; }

            /* Badges */
            .badge.bg-light { background: var(--background-alt) !important; color: var(--text-primary) !important; border: 1px solid var(--border-color); }

            /* Buttons and links */
            .btn-link { color: var(--primary-color); text-decoration: none; font-weight: 600; }
            .btn-link:hover { text-decoration: underline; }

            /* Headings spacing */
            .section-title { margin-bottom: 1rem; }
            
            /* Animations */
            @keyframes fadeInUp {
                from {
                    opacity: 0;
                    transform: translateY(30px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
            
            .fade-in-up {
                animation: fadeInUp 0.8s ease;
            }
            
            /* Hero minimal */
            .hero {
                border-radius: 18px;
                background: linear-gradient(180deg, var(--background-alt), var(--surface));
                border: 1px solid var(--border-color);
            }
            
            /* Responsive */
            @media (max-width: 768px) {
                .navbar-brand {
                    font-size: 1.5rem;
                }
                
                .btn {
                    padding: 0.6rem 1.5rem;
                    font-size: 0.85rem;
                }
                
                
            }
        </style>
    {% endblock %}
</head>
<body>
    <!-- Navigation -->
    {% if app.user %}
        {# Navigation pour utilisateurs connectés #}
        <nav class="navbar navbar-expand-lg navbar-light sticky-top">
            <div class="container">
                <a class="navbar-brand brand-text" href="{{ is_granted('ROLE_ADMIN') ? path('app_admin_dashboard') : path('app_profile') }}">
                    <i class="bi bi-bag-check-fill"></i> FreshMarket
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto align-items-center">
                        {% if not is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_cart_index') }}">
                                    <i class="bi bi-cart3"></i> Panier
                                </a>
                            </li>
                        {% endif %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                                {% if app.user.avatar %}
                                    <img src="{{ asset('uploads/' ~ app.user.avatar) }}" class="rounded-circle me-2" style="width:35px;height:35px;object-fit:cover;border:2px solid var(--primary-color);">
                                {% else %}
                                    <i class="bi bi-person-circle fs-4 me-2"></i>
                                {% endif %}
                                <span>{{ app.user.fullName }}</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li><a class="dropdown-item" href="{{ path('app_admin_dashboard') }}"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_products') }}"><i class="bi bi-box-seam"></i> Produits</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_orders') }}"><i class="bi bi-cart-check"></i> Commandes</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_admin_reservations') }}"><i class="bi bi-calendar-event"></i> Réservations</a></li>
                                {% else %}
                                    <li><a class="dropdown-item" href="{{ path('app_profile') }}"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_profile_edit') }}"><i class="bi bi-person-gear"></i> Mon Profil</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_profile_reservations') }}"><i class="bi bi-calendar-check"></i> Mes Réservations</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_profile_orders') }}"><i class="bi bi-basket"></i> Mes Commandes</a></li>
                                {% endif %}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_home') }}"><i class="bi bi-house"></i> Voir le site</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right"></i> Déconnexion</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    {% else %}
        {# Navigation pour visiteurs - PREMIUM #}
        <nav class="navbar navbar-expand-lg navbar-premium sticky-top">
            <div class="container">
                <a class="navbar-brand-premium" href="{{ path('app_home') }}">
                    <div class="brand-icon-wrapper">
                        <i class="bi bi-bag-check-fill"></i>
                    </div>
                    <span class="brand-name">Fresh<span class="brand-highlight">Market</span></span>
                </a>
                <button class="navbar-toggler-premium" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav-premium mx-auto">
                        <li class="nav-item-premium">
                            <a class="nav-link-premium {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">
                                <i class="bi bi-house-heart-fill"></i>
                                <span>Accueil</span>
                            </a>
                        </li>
                        <li class="nav-item-premium">
                            <a class="nav-link-premium {{ app.request.get('_route') starts with 'app_product' ? 'active' : '' }}" href="{{ path('app_product_index') }}">
                                <i class="bi bi-basket-fill"></i>
                                <span>Boutique</span>
                            </a>
                        </li>
                        <li class="nav-item-premium">
                            <a class="nav-link-premium {{ app.request.get('_route') == 'app_about' ? 'active' : '' }}" href="{{ path('app_about') }}">
                                <i class="bi bi-info-circle-fill"></i>
                                <span>À propos</span>
                            </a>
                        </li>
                        <li class="nav-item-premium">
                            <a class="nav-link-premium {{ app.request.get('_route') == 'app_contact' ? 'active' : '' }}" href="{{ path('app_contact') }}">
                                <i class="bi bi-envelope-heart-fill"></i>
                                <span>Contact</span>
                            </a>
                        </li>
                    </ul>
                    
                    <ul class="navbar-actions-premium">
                        <li class="nav-item-premium">
                            <form class="search-form-premium" action="{{ path('app_product_search') }}" method="get">
                                <div class="search-wrapper">
                                    <i class="bi bi-search search-icon"></i>
                                    <input type="search" name="q" placeholder="Rechercher..." class="search-input-premium">
                                </div>
                            </form>
                        </li>
                        <li class="nav-item-premium">
                            <a class="btn-nav-premium btn-nav-cart" href="{{ path('app_cart_index') }}" title="Mon Panier">
                                <i class="bi bi-cart3"></i>
                                <span class="cart-badge">0</span>
                            </a>
                        </li>
                        <li class="nav-item-premium">
                            <a class="btn-nav-premium btn-nav-outline" href="{{ path('app_login') }}">
                                <i class="bi bi-box-arrow-in-right"></i>
                                <span>Connexion</span>
                            </a>
                        </li>
                        <li class="nav-item-premium">
                            <a class="btn-nav-premium btn-nav-solid" href="{{ path('app_register') }}">
                                <i class="bi bi-person-plus-fill"></i>
                                <span>Inscription</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    {% endif %}

    <!-- Flash Messages -->
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} alert-dismissible fade show m-3" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endfor %}

    <!-- Main Content -->
    <main class="container my-4">
        {% block body %}{% endblock %}
    </main>

    <!-- Footer Premium -->
    <footer class="footer-premium">
        <div class="container">
            <div class="footer-main">
                <div class="row g-5">
                    <!-- Brand Column -->
                    <div class="col-lg-4 col-md-6">
                        <div class="footer-brand">
                            <div class="footer-logo">
                                <div class="footer-logo-icon">
                                    <i class="bi bi-bag-check-fill"></i>
                                </div>
                                <span class="footer-logo-text">Fresh<span class="highlight">Market</span></span>
                            </div>
                            <p class="footer-description">
                                Votre marketplace de confiance pour des produits frais et de qualité, livrés directement chez vous. Excellence et satisfaction garanties.
                            </p>
                            <div class="social-links-premium">
                                <a href="#" class="social-link" aria-label="Facebook">
                                    <i class="bi bi-facebook"></i>
                                </a>
                                <a href="#" class="social-link" aria-label="Twitter">
                                    <i class="bi bi-twitter"></i>
                                </a>
                                <a href="#" class="social-link" aria-label="Instagram">
                                    <i class="bi bi-instagram"></i>
                                </a>
                                <a href="#" class="social-link" aria-label="LinkedIn">
                                    <i class="bi bi-linkedin"></i>
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Navigation Column -->
                    <div class="col-lg-2 col-md-6">
                        <h5 class="footer-title">Navigation</h5>
                        <ul class="footer-links">
                            <li><a href="{{ path('app_home') }}"><i class="bi bi-chevron-right"></i>Accueil</a></li>
                            <li><a href="{{ path('app_product_index') }}"><i class="bi bi-chevron-right"></i>Boutique</a></li>
                            <li><a href="{{ path('app_about') }}"><i class="bi bi-chevron-right"></i>À propos</a></li>
                            <li><a href="{{ path('app_contact') }}"><i class="bi bi-chevron-right"></i>Contact</a></li>
                        </ul>
                    </div>

                    <!-- Catégories Column -->
                    <div class="col-lg-3 col-md-6">
                        <h5 class="footer-title">Catégories</h5>
                        <ul class="footer-links">
                            <li><a href="{{ path('app_product_category', {category: 'plat'}) }}"><i class="bi bi-chevron-right"></i>Plats</a></li>
                            <li><a href="{{ path('app_product_category', {category: 'boisson'}) }}"><i class="bi bi-chevron-right"></i>Boissons</a></li>
                            <li><a href="{{ path('app_product_category', {category: 'dessert'}) }}"><i class="bi bi-chevron-right"></i>Desserts</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a href="{{ path('app_admin_dashboard') }}"><i class="bi bi-chevron-right"></i>Administration</a></li>
                            {% endif %}
                        </ul>
                    </div>

                    <!-- Contact Column -->
                    <div class="col-lg-3 col-md-6">
                        <h5 class="footer-title">Contact</h5>
                        <ul class="footer-contact">
                            <li>
                                <i class="bi bi-geo-alt-fill"></i>
                                <div>
                                    <strong>Adresse</strong>
                                    <span>123 Rue Example, 75000 Paris</span>
                                </div>
                            </li>
                            <li>
                                <i class="bi bi-telephone-fill"></i>
                                <div>
                                    <strong>Téléphone</strong>
                                    <span>+33 1 23 45 67 89</span>
                                </div>
                            </li>
                            <li>
                                <i class="bi bi-envelope-fill"></i>
                                <div>
                                    <strong>Email</strong>
                                    <span>contact@freshmarket.fr</span>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Footer Bottom -->
            <div class="footer-bottom">
                <div class="row align-items-center">
                    <div class="col-md-6 text-center text-md-start">
                        <p class="footer-copyright">
                            © {{ 'now'|date('Y') }} <strong>FreshMarket</strong>. Tous droits réservés.
                        </p>
                    </div>
                    <div class="col-md-6">
                        <ul class="footer-legal">
                            <li><a href="#">Conditions d'utilisation</a></li>
                            <li><a href="#">Politique de confidentialité</a></li>
                            <li><a href="#">Mentions légales</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        
        <script>
            // Navbar scroll effect
            window.addEventListener('scroll', function() {
                const navbar = document.querySelector('.navbar-premium');
                if (navbar) {
                    if (window.scrollY > 50) {
                        navbar.classList.add('scrolled');
                    } else {
                        navbar.classList.remove('scrolled');
                    }
                }
            });
        </script>
        
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</body>
</html>
